# Supra Blockchain Metrics Exporter Configuration

# Role of this exporter instance
# Options: validator, rpc, both
ROLE=validator

# Validator Configuration (required if ROLE=validator or ROLE=both)
VALIDATOR_LOG_PATH=/path/to/your/validator.log
NETWORK_PUBKEY=your_validator_public_key_here

# RPC Configuration (required if ROLE=rpc or ROLE=both)
RPC_URL=http://localhost:27000/rpc/v1
RPC_LOG_PATH=/path/to/your/rpc.log

# Exporter Configuration
EXPORTER_PORT=7897
PUBLIC_RPC_URL=https://rpc-mainnet.supra.com/rpc/v1

# Optional Configuration
POLL_INTERVAL=10
